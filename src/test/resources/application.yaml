spring:
  datasource:
    url: jdbc:h2:mem:mydb
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect

logging:
  level:
    root: OFF
    com.sda.clinicapi: DEBUG


springdoc:
  api-docs:
    enabled: false